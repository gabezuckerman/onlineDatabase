#uses dplyr to add to existing database files, whilst retaining raw data 
#could try multidplyr when creating all files.
#should just be one or two lines per table, then a more complex merge statement to add to all